{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm run build"
  },
  "deploy": {
    "preDeployCommand": "if [ -n \"$APP_KEY\" ] && [ -n \"$DB_HOST\" ] && [ -n \"$DB_DATABASE\" ] && [ -n \"$DB_USERNAME\" ]; then php artisan migrate --force; else echo 'Skipping migrate because Railway variables are not complete yet.'; fi",
    "startCommand": "php artisan serve --host=0.0.0.0 --port=${PORT:-8080}",
    "healthcheckPath": "/up",
    "healthcheckTimeout": 180,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
